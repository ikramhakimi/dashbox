RewriteEngine On

# Keep direct access for existing files and directories.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route everything else through the front controller.
RewriteRule ^ index.php [L,QSA]
